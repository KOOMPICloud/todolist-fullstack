# KOOMPI Provider OAuth Configuration
# These will be automatically injected when deployed on KConsole

VITE_KOOMPI_PROVIDER_CLIENT_ID=""
VITE_KOOMPI_PROVIDER_CLIENT_SECRET=""
VITE_KOOMPI_PROVIDER_REDIRECT_URI=""

# API Configuration (for development)
VITE_API_URL="http://localhost:3001"

# Backend Configuration
DATABASE_PATH="/data/db/app.db"
NODE_ENV="production"
PORT="3001"
