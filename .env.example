# KOOMPI Provider OAuth Credentials
# These will be injected automatically when deployed on KConsole
# NEXT_PUBLIC_ prefix is required for Next.js client-side access

NEXT_PUBLIC_KOOMPI_PROVIDER_CLIENT_ID="kcp_mlgab946_1ec2ab34"
NEXT_PUBLIC_KOOMPI_PROVIDER_CLIENT_SECRET="93024ea8361580fc8e85e4e6a4d6dcffab87d63ada456e9674dca1e358e33ca1"
NEXT_PUBLIC_KOOMPI_PROVIDER_REDIRECT_URI="https://test-nextjs-template-62c5.tunnel.koompi.cloud/auth/callback"

# Database (SQLite at /data/db for persistence)
DATABASE_PATH="/data/db/app.db"

# App Configuration
NODE_ENV="production"
PORT=3000
